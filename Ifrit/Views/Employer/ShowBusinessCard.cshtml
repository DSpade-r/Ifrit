@model Ifrit.Models.UIBusinessCard

@{
    ViewBag.Title = "ShowBusinessCard";
}
@*<h2></h2>

<p>
    @Html.ActionLink("Создать новое", "Create")
</p>
<h2>@Html.DisplayFor(model => model.Title)</h2>
<p>
    @Html.Raw("<img style='width:80px; height:60px;' src=\"data:image/jpeg;base64,"
                                + Convert.ToBase64String(Model.Logo) + "\" />")
</p>
<h6>
    @Html.DisplayFor(model => model.Description)
</h6>*@
<div class="row">
    <div class="col-sm-10 col-md-10">
        <div class="jumbotron">
            @Html.Raw("<img style='width:800px' src=\"data:image/jpeg;base64,"
                                + Convert.ToBase64String(Model.Logo) + "\" />")
            <div class="caption">
                <h3>@Html.DisplayFor(model => model.Title)</h3>
                <p>@Html.DisplayFor(model => model.Description)</p>
                @*<p><a href="#" class="btn btn-primary" role="button">Кнопка</a> <a href="#" class="btn btn-default" role="button">Кнопка</a></p>*@
            </div>
        </div>
    </div>
</div>

@*@Html.ActionLink("Редактировать", "Edit", new { id = item.ResumeId }) |
    @Html.ActionLink("Подробности", "Details", new { id = item.ResumeId }) |
    @Html.ActionLink("Удалить", "Delete", new { id = item.ResumeId })*@
